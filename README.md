# Argos

Harness global-first para Claude Code. El motor vive una vez en ~/.claude; cada repo aporta solo argos.config.json; el equipo recibe un compilado opcional (argos export). Ver specs/0001-arquitectura-global-first.md.
